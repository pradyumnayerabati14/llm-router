This Project is an LLM Router.
The main idea: Some LLM's are very costly and some are very cheap. Not every query that user asks need to be sent to a costly model. Simple questions can be sent to a cheaper model to optimize tokens and therebt cost.

This Project is based proposes an LLM router, which efficiently routes a user query to a suitable model, there by optimizing both cost and quality.